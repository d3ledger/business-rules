version: '3.1'

services:
  brvs:
    container_name: brvs
    build:
      context: ../
      dockerfile: Dockerfile
    networks:
      - deploy_d3-network

networks:
  deploy_d3-network:
    external: true
