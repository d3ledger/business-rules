version: '3.1'

services:
  brvs:
    container_name: brvs
    build:
      context: ../
      dockerfile: Dockerfile
    networks:

networks:
  default:
    # Specify the network Iroha is present in if needed
    deploy_d3-network:
      external: true
